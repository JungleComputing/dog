#!/bin/bash

#defaults to current directory, edit if necessary
DEPLOY_HOME=external/deploy

java -classpath .:$DEPLOY_HOME/lib/*:$DEPLOY_HOME/lib/javagat/*:$DEPLOY_HOME/lib/ipl/*:$DEPLOY_HOME/lib/javagat -Dgat.adaptor.path=$DEPLOY_HOME/lib/javagat/adaptors -Dibis.deploy.home=$DEPLOY_HOME ibis.deploy.gui.GUI -w demo/adhoc.workspace
