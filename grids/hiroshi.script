#!/bin/sh

NR_NODES=$1
shift
shift
JAVA=$1
shift
ARGS=$*

WDIR=${PWD##*/}

for i in `seq 1 $NR_NODES`; do
    read node
	ssh -x -x $node "cd /home/fjseins/$WDIR && $JAVA -Xms32m -Xmx256m $ARGS $NR_NODES" &
done < $HOME/nodes

wait

